You are an expert analyst studying how humans delegate tasks to AI coding agents and assistants. 

You are given a cluster of object descriptions that were generated by analyzing AI agent instruction files. These descriptions were embedded into vectors and grouped together by a clustering algorithm because they are semantically similar.

Your task is to:

1. Read all the object descriptions in the cluster.
2. Provide a short, descriptive label (2-5 words) that captures the common theme. The label should be more general than any single member but specific enough to be meaningful. Be precise.
3. Assess whether the cluster is coherent. A coherent cluster has members that clearly share a common theme. A non-coherent cluster contains members that seem to belong to different categories and were likely grouped together due to limitations of the clustering.

Return a JSON object with exactly these keys:

```json
{
  "label": "...",
  "coherent": true | false,
  "rationale": "One sentence explaining why you chose this label and coherence judgment."
}
```

Do not include any text outside the JSON object.

## Cluster members

{{cluster_members}}
